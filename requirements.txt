# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36
pgvector==0.3.6
psycopg2-binary==2.9.10

# Embeddings
sentence-transformers>=3.3.1
torch>=2.5.1  # version resolved by uv based on your Python ABI

# LLM — Groq (default) via OpenAI-compatible SDK
openai>=1.59.3

# Validation
pydantic==2.10.3
pydantic-settings==2.7.0

# Observability
monocle-apptrace>=0.7.3  # Okahu Cloud tracing (rebranded from okahu-apptrace)
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0

# RAGAS per-agent evaluation
ragas>=0.2.0,<0.3.0
langchain-openai>=0.1.0  # Groq-compatible LLM wrapper for RAGAS

# Utilities
python-dotenv==1.0.1
cachetools>=5.3.0
httpx==0.28.1
tenacity==9.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
pytest-cov==6.0.0
json-repair>=0.30.0
